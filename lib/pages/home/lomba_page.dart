import 'package:flutter/material.dart';

class LombaPage extends StatelessWidget {
  const LombaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Lomba Page', style: TextStyle(fontSize: 24))),
    );
  }
}
