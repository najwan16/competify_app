import 'package:flutter/material.dart';

class MentorPage extends StatelessWidget {
  const MentorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Mentor Page', style: TextStyle(fontSize: 24))),
    );
  }
}
